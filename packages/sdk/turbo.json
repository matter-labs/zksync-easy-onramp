{
  "extends": ["//"],
  "tasks": {
    "build:watch": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["typecheck", "^build"],
      "outputs": ["dist/**"],
      "env": ["API_URL", "PRIVATE_KEY", "PUBLISH"]
    },
    "typecheck": {}
  }
}
